# Code generated by protoc-gen-gohttp. DO NOT EDIT.
# source: proto/gosearch/contracts/service.proto
openapi: 3.0.3
info:
  title: gosearch
  version: '1.0'
paths:
  /documents:
    post:
      tags:
        - documents
      summary: create new document
      description: creates and indexes a new document
      parameters:
      requestBody:
        description: CreateIndexedDocumentCommand
        content:
          application/json:
            schema:
              type: object
              properties:
              $ref: '#/components/schemas/CreateIndexedDocumentCommand'
        required: true
      responses:
        '200':
          description: DocumentCreatedResponse
          content: 
            application/json:
              schema:
                $ref: '#/components/schemas/DocumentCreatedResponse'
  /documents/search:
    get:
      tags:
        - documents
      summary: searches documents
      description: searches document with provided search query
      parameters:
        - in: query
          name: query
          required: true
          schema:
            type: string
            example: sample
      responses:
        '200':
          description: SearchResponse
          content: 
            application/json:
              schema:
                $ref: '#/components/schemas/SearchResponse'
components:
  schemas:
    DocumentCreatedResponse:
      type: object
      properties:
        documents:
          type: array
          items:
            $ref: '#/components/schemas/IndexedDocument'
    IndexedDocument:
      type: object
      properties:
        documentId:
          type: integer
          format: int64
          example: 1
        indexedTerms:
          type: array
          items:
            type: string
            example: sample
    CreateIndexedDocumentCommand:
      type: object
      properties:
        documents:
          type: array
          items:
            type: object
    SearchResponse:
      type: object
      properties:
        documents:
          type: array
          items:
            type: object
    SearchQuery:
      type: object
      properties:
        query:
          type: string
          example: sample
